<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="follow">
	<select id="followerList" resultType="followDTO">
		select m.member_nick, m.member_img, m.id, f.id fg_member_id, fw_member_id
		from tbl_member m join tbl_follow f
		on fg_member_id = #{id}
	</select>
	
	<select id="folloingList" resultType="followDTO">
		select m.member_nick, m.member_img, m.id, f.id fg_member_id, fw_member_id
		from tbl_member m join tbl_follow f
		on fw_member_id = #{id}
	</select>
</mapper>