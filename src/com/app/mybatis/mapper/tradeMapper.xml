<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="trade">

	<select id="list" resultType="TradeDTO">
		select board_title, board_contents, board_date, board_status, member_id from tbl_trade
	</select>
	
	<select id="insert">
		insert into tbl_trade (board_title, board_date, board_status, member_id)
		values (#{boardtitle}, now(), #{boardstatus}, #{memberid})
	</select>
	
</mapper>