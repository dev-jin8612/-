<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="checkId" resultType="_int">
		select count(id) from member where id = #{id}
	</select>

	<!-- 임시로 넣은 정보들을 나중에 수정하기 -->
	<insert id="insert">
		insert into member
		(id, member_pw, member_name,member_img, member_hp)
		values(#{id}, #{memberpw}, #{membername},"임시파일", "010-")
	</insert>

	<select id="login" resultType="string">
		select id from member where id = #{id} and member_pw = #{memberpw}
	</select>
	<select id="profile" resultType="member2VO">
		select id, member_id, member_password, member_name, member_nick, member_img, member_hp
		from tbl_member where id = #{id}
	</select>
</mapper>